spring:
  datasource:
    url: jdbc:postgresql://188.225.84.177:5432/default_db
    username: gen_user
    password: g1buXYhff/GC=D
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
